{
  "name": "xen",
  "version": "1.0.0",
  "description": "My epic Nuxt.js project",
  "author": "takashi",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "build:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt build && rm -rf docs && mv dist docs",
    "generate:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt generate && rm -rf docs && mv dist docs"
  },
  "dependencies": {
    "@nuxtjs/pwa": "^3.0.0-beta.16",
    "bootstrap": "^4.1.3",
    "bootstrap-vue": "^2.0.0-rc.11",
    "cross-env": "^5.2.0",
    "nuxt": "^2.4.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.9"
  }
}
